# Async/Await & Promises

**Week 6** · Wed, Sep 02, 2026 · Core · Module 1: JS Fundamentals
**Session Owner:** Prachi

## Learning Objectives

Synchronous vs Asynchronous execution, Promises, `async` and `await` keywords in plain English.

## For Session Owners

- Prepare talking points and any live-demo code around the objectives above.
- Add supporting material (slides, sample code, recordings, cheat sheets) to this `Artifacts` folder.
- Review participant submissions in the sibling `../<ParticipantName>/` folders after the session.

## Weekly Assignment (Homework)

1) Create a mock `fetchUserData()` function using `setTimeout` to simulate delay, then call it using `async/await` and log results.

2) Write a second mock function `fetchOrderData()` and call both functions sequentially with `await`, logging total time taken.

3) Introduce a deliberate rejected Promise and handle it with `try/catch`, logging a friendly error message.

## Submission Instructions

- Save your files inside `Sessions/Week-06-Async-Await-Promises/<YourName>/` (your own participant folder).
- Use plain, descriptive filenames as instructed above.
- Do not commit `node_modules/` or other generated folders.
