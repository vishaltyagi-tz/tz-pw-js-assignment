# DOM Basics

**Optional Session E** · Self-paced, anytime after Week 7 · Optional · Module 2: Playwright Core
**Session Owner:** Self-study

## Learning Objectives

Chrome DevTools usage for element inspection, HTML attributes (id, class, data-*), DOM hierarchy and parent-child relationships, understanding how browsers render pages.

## For Session Owners

- Prepare talking points and any live-demo code around the objectives above.
- Add supporting material (slides, sample code, recordings, cheat sheets) to this `Artifacts` folder.
- Review participant submissions in the sibling `../<ParticipantName>/` folders after the session.

## Weekly Assignment (Homework)

1) Use Chrome DevTools to inspect 5 different elements on a webpage - document their tag names, attributes, and position in the DOM hierarchy.

2) Practice using the Elements panel to temporarily modify HTML/CSS and observe changes in real-time.

3) Use the Console to run `document.querySelector()` commands and locate elements by various attributes, comparing results to Playwright locators.

## Submission Instructions

- Save your files inside `Sessions/Optional-E-DOM-Basics/<YourName>/` (your own participant folder).
- Use plain, descriptive filenames as instructed above.
- Do not commit `node_modules/` or other generated folders.
