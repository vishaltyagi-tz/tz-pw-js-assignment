# Actions & User Inputs

**Week 9** · Wed, Sep 23, 2026 · Core · Module 2: Playwright Core
**Session Owner:** Tarun

## Learning Objectives

Filling inputs (`.fill()`), clicking (`.click()`), radio buttons (`.check()`), dropdowns (`.selectOption()`).

## For Session Owners

- Prepare talking points and any live-demo code around the objectives above.
- Add supporting material (slides, sample code, recordings, cheat sheets) to this `Artifacts` folder.
- Review participant submissions in the sibling `../<ParticipantName>/` folders after the session.

## Weekly Assignment (Homework)

1) Automate filling out a full registration web form with text fields, checkboxes, radio options, and multi-select dropdowns.

2) Add keyboard interactions (`.press('Tab')`, `.press('Enter')`) to move between fields and submit the form without clicking the submit button directly.

3) Write a negative-path test that submits the form with required fields left blank and verify the appropriate validation messages appear.

## Submission Instructions

- Save your files inside `Sessions/Week-09-Actions-User-Inputs/<YourName>/` (your own participant folder).
- Use plain, descriptive filenames as instructed above.
- Do not commit `node_modules/` or other generated folders.
