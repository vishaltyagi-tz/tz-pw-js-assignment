# Transitioning JS to TypeScript

**Week 16** · Wed, Nov 11, 2026 · Core · Module 4: TypeScript Intro
**Session Owner:** Vishal Tyagi

## Learning Objectives

TypeScript intro, `.ts` files, basic type annotations (`string`, `number`, `boolean`, `any`), compile errors vs runtime errors.

## For Session Owners

- Prepare talking points and any live-demo code around the objectives above.
- Add supporting material (slides, sample code, recordings, cheat sheets) to this `Artifacts` folder.
- Review participant submissions in the sibling `../<ParticipantName>/` folders after the session.

## Weekly Assignment (Homework)

1) Rename existing JS helper files to `.ts`, add explicit type annotations to functions/variables, and resolve all TypeScript compiler warnings.

2) Deliberately pass a wrong-typed argument (e.g. a number where a string is expected) to one function and document the compiler error message.

3) Replace any remaining `any` types with more specific types and note which ones were safe to change.

## Submission Instructions

- Save your files inside `Sessions/Week-16-Transitioning-JS-to-TypeScript/<YourName>/` (your own participant folder).
- Use plain, descriptive filenames as instructed above.
- Do not commit `node_modules/` or other generated folders.
