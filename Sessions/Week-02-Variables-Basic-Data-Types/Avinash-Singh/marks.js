let marks = 85;
// boolean isPassed = marks >= 40; // This line is commented out because the syntax is incorrect. In JavaScript, we use 'let' or 'const' to declare variables, not 'boolean'.
let isPassed = marks >= 40;
console.log(isPassed);
