# Playwright Setup & CodeGen Intro

**Week 7** · Wed, Sep 09, 2026 · Core · Module 2: Playwright Core
**Session Owner:** Hritik

## Learning Objectives

`npm init playwright@latest`, project structure, running Playwright CodeGen (`npx playwright codegen`).

## For Session Owners

- Prepare talking points and any live-demo code around the objectives above.
- Add supporting material (slides, sample code, recordings, cheat sheets) to this `Artifacts` folder.
- Review participant submissions in the sibling `../<ParticipantName>/` folders after the session.

## Weekly Assignment (Homework)

1) Use CodeGen to record a complete login & product search scenario on SauceDemo. Run the generated script and inspect results.

2) Record a second CodeGen flow: adding an item to the cart and completing checkout, then clean up the generated code by removing redundant steps.

3) Explore the generated `playwright.config.ts` and document (in comments) what `baseURL`, `headless`, and `retries` control.

## Submission Instructions

- Save your files inside `Sessions/Week-07-Playwright-Setup-CodeGen-Intro/<YourName>/` (your own participant folder).
- Use plain, descriptive filenames as instructed above.
- Do not commit `node_modules/` or other generated folders.
