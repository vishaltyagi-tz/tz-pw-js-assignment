# Functions & Reusability

**Week 4** · Wed, Aug 19, 2026 · Core · Module 1: JS Fundamentals
**Session Owner:** Prachi

## Learning Objectives

Function declarations, parameters, return values, arrow functions (`() => {}`).

## For Session Owners

- Prepare talking points and any live-demo code around the objectives above.
- Add supporting material (slides, sample code, recordings, cheat sheets) to this `Artifacts` folder.
- Review participant submissions in the sibling `../<ParticipantName>/` folders after the session.

## Weekly Assignment (Homework)

1) Write reusable helper functions: `calculateTotalPrice(price, taxRate)` and `generateUserEmail(firstName, domain)`.

2) Rewrite both functions as arrow functions and compare the syntax in a comment.

3) Write a `isValidPassword(password)` function that returns `true`/`false` based on a minimum length rule, and call it with 3 different test values.

## Submission Instructions

- Save your files inside `Sessions/Week-04-Functions-Reusability/<YourName>/` (your own participant folder).
- Use plain, descriptive filenames as instructed above.
- Do not commit `node_modules/` or other generated folders.
